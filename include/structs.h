#pragma once

// Default constructors ? 

struct Color {
    float  r;
    float  g;
    float  b;
    float  a;
};

struct Coordinates {
    float   x;
    float   y;
    float   z;
    bool    exists = true;
};

// Create a new struct for intersections with distance and other data ?